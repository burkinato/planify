# Planify Codex Context

This legacy `.agent` directory came from Antigravity-era workflows.

For Codex work, use:

- `../AGENTS.md` for the short root guide.
- `../.codex/PROJECT.md` for product and architecture context.
- `../.codex/TESTING.md` for login and verification notes.
- `../.codex/DEPLOYMENT.md` for server and release flow.

Do not load the old `.agent` subfolders wholesale. They are retained only as
historical reference unless a human explicitly asks for them.
